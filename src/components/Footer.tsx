import React from "react";
import styled from "styled-components";

const Footer: React.FC = () => {
  const StyledFooter = styled.footer`
    font-size: 12px;
  `;

  return <StyledFooter>Coded by Heraldo Fortuna</StyledFooter>;
};

export default Footer;
